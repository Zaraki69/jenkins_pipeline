pipeline // declarative pipeline always starts with keyword 'pipeline'
{
    agent any
    stages // all the stages of the pipelines
    {
        stage ("print a message") // name of a stage
        {
            steps { echo "jenkins by shubham gajjewar"} // things to perform in a stage is a step

        }
    }
}
 
